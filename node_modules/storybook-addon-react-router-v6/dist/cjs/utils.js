"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.defer = defer;
exports.generateAppUrl = void 0;

var _reactRouterDom = require("react-router-dom");

var generateAppUrl = function generateAppUrl(path, params, search, hash) {
  var pathname = (0, _reactRouterDom.generatePath)(path, params);
  var queryString = search && Object.keys(search).length > 0 ? "?".concat(new URLSearchParams(search).toString()) : '';
  var hashString = hash ? hash : '';
  return "".concat(pathname).concat(queryString).concat(hashString);
};

exports.generateAppUrl = generateAppUrl;

function defer() {
  var deferred = {};
  deferred.promise = new Promise(function (resolve, reject) {
    deferred.resolve = resolve;
    deferred.reject = reject;
  });
  return deferred;
}