function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function _iterableToArrayLimit(arr, i) { var _i = arr == null ? null : typeof Symbol !== "undefined" && arr[Symbol.iterator] || arr["@@iterator"]; if (_i == null) return; var _arr = []; var _n = true; var _d = false; var _s, _e; try { for (_i = _i.call(arr); !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

import React, { useState } from "react";
import { AddonPanel } from "@storybook/components";
import { PanelContent } from "./components/PanelContent";
import { useChannel } from '@storybook/api';
import { EVENTS } from "./constants";
import { STORY_CHANGED } from "@storybook/core-events";
export var Panel = function Panel(props) {
  var _useChannel;

  var _useState = useState([]),
      _useState2 = _slicedToArray(_useState, 2),
      navigationEvents = _useState2[0],
      setNavigationEvents = _useState2[1];

  useChannel((_useChannel = {}, _defineProperty(_useChannel, EVENTS.ROUTE_MATCHES, function (event) {
    setNavigationEvents(function (prev) {
      return [].concat(_toConsumableArray(prev), [event]);
    });
  }), _defineProperty(_useChannel, EVENTS.NAVIGATION, function (event) {
    setNavigationEvents(function (prev) {
      return [].concat(_toConsumableArray(prev), [event]);
    });
  }), _defineProperty(_useChannel, EVENTS.STORY_LOADED, function (event) {
    setNavigationEvents(function (prev) {
      return [].concat(_toConsumableArray(prev), [event]);
    });
  }), _defineProperty(_useChannel, STORY_CHANGED, function () {
    setNavigationEvents([]);
  }), _useChannel));

  var clear = function clear() {
    props.api.emit(EVENTS.CLEAR);
    setNavigationEvents([]);
  };

  return /*#__PURE__*/React.createElement(AddonPanel, props, /*#__PURE__*/React.createElement(PanelContent, {
    navigationEvents: navigationEvents,
    onClear: clear
  }));
};